const initialState = {
  question: "Pour ou contre la congolemexicalisation ?",
  oui: 0,
  non: 0
}

export default (state = initialState, action) => {
  // TODO: traiter les actions
  return state;
}
